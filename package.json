{
  "name": "spendtron",
  "version": "1.0.0",
  "description": "GitHub Actions cost auditor — ranks a private org's workflows by real compute cost via MCP",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/node": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "@neondatabase/serverless": "^1.1.0",
    "@octokit/auth-app": "^8.0.4",
    "@octokit/rest": "^22.0.1",
    "express": "^5.0.0",
    "stripe": "^22.5.0",
    "zod": "^4.6.5"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
